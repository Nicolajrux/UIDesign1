git clone https://github.com/Nicolajrux/REPOSITORY # clones a repository
git status # displays possible files which can be added to repo
git config --global user.name "username" # updates the user name same with email
git add FILENAME # adds filename
git commit -m "MESSAGE" # commits the updated version of your file
git push origin # pushes git to the hub

# usually like this :

git add FILENAME
git commit -m "Updated bibabbub"
git push origin
# enter username
Nicolajrux
#enter token
dsjnsonsfnsdj look it up !

